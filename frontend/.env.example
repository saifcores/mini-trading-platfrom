# Optional: backend base URL (no trailing slash). When unset, the app uses built-in demo data.
# VITE_API_URL=http://localhost:8080

# Same-origin API via Vite dev proxy (see vite.config): forward /v1 -> backend.
# VITE_DEV_PROXY_TARGET=http://localhost:8080
# VITE_API_RELATIVE=1
